<!-- search bar -->
<div class="container search-bar">
    <form action="/search">
        <div class="input-group">
            <input type="text" name="keyword" class="form-control" placeholder="輸入餐廳、分類" aria-label="Restaurant Name..."
                value="{{keyword}}" aria-describedby="search-button">
            <div class="input-group-append">
                <button class="btn btn-major" type="submit" id="search-button"><i class="fas fa-search"></i></button>
            </div>
        </div>
        <div class="mt-5">
            <select class="form-select" aria-label="Default select example" style="width: 180px;" id="select">
                <option selected>sort</option>
                <option value="1">A -> Z</option>
                <option value="2">Z -> A</option>
                <option value="3">類別</option>
                <option value="3">地區</option>
            </select>
        </div>
    </form>
</div>


<!-- restaurant list -->
<div class="container mt-5">
    <div class="row">
        <div class="col-md-10 col-12 mx-auto mb-3">
            <div class="card-columns">
                {{ error }}
                {{#each restaurant}}
                <div>
                    <div class="card mb-1">
                        <img class="card-img-top" src="{{this.image}}" alt="{{this.name}}">
                        <div class="card-body p-3">
                            <h6 class="card-title mb-1">{{this.name}}</h6>

                            <div class="restaurant-category mb-1">
                                <i class="fas fa-utensils pr-2"></i> {{this.category}}
                            </div>

                            <span class="badge badge-pill badge-danger font-weight-normal">
                                {{this.rating}}
                                <i class="fas fa-star fa-xs"></i>
                            </span>
                        </div>
                    </div>
                    <div class="mb-3 d-flex justify-content-end">
                        <a href="/restaurants/{{this._id}}/edit" role="button">Edit</a>
                        <a href="/restaurants/{{this._id}}" class="text-secondary ml-2" role="button">Detail</a></a>
                        <form action="./restaurants/{{this._id}}/delete" method="POST" style="display: inline;"
                            class="ml-2">
                            <button type="submit">Delete</button>
                        </form>
                    </div>
                </div>
                {{/each}}
            </div>
        </div>
    </div>
</div>